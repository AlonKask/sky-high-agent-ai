import React from 'react';
import { Layout } from '@/components/Layout';
import { UnifiedQuoteBuilder } from '@/components/UnifiedQuoteBuilder';

const UnifiedQuoteBuilderPage = () => {
  return (
    <Layout>
      <UnifiedQuoteBuilder />
    </Layout>
  );
};

export default UnifiedQuoteBuilderPage;